<UserControl x:Class="GarlicSaveMgr.UpdaterModule.UpdaterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Tag="updater">
    <Grid Margin="8,10,8,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Text="Actualización de Garlic SaveMgr" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" />
                <TextBlock x:Name="VersionLabel" Margin="0,4,0,0" Foreground="{DynamicResource TextSecondary}" />
            </StackPanel>
            <TextBlock x:Name="StatusLabel" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}" />
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Release más reciente" Foreground="{DynamicResource TextSecondary}" />
                <TextBlock x:Name="ReleaseLabel" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="CheckButton" Width="155" Height="38" Click="CheckButton_Click">Buscar actualización</Button>
                <Button x:Name="UpdateButton" Width="155" Height="38" Margin="8,0,0,0" Style="{StaticResource PrimaryButton}" Click="UpdateButton_Click" IsEnabled="False">Actualizar</Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" BorderBrush="{DynamicResource BorderSoft}" BorderThickness="1" CornerRadius="{DynamicResource Theme.ComboBoxItemCornerRadius}" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReleaseBody" TextWrapping="Wrap" Foreground="{DynamicResource TextPrimary}" />
            </ScrollViewer>
        </Border>

        <Border x:Name="ConfirmPanel" Grid.Row="3" Visibility="Collapsed" Margin="0,10,0,0" BorderBrush="{DynamicResource BorderSoft}" BorderThickness="1" CornerRadius="{DynamicResource Theme.ComboBoxItemCornerRadius}" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="ConfirmText" TextWrapping="Wrap" Foreground="{DynamicResource TextPrimary}" />
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="ConfirmCancelButton" Width="105" Height="34" Click="ConfirmCancelButton_Click">Cancelar</Button>
                    <Button x:Name="ConfirmContinueButton" Width="135" Height="34" Margin="8,0,0,0" Style="{StaticResource PrimaryButton}" Click="ConfirmContinueButton_Click">Instalar actualización</Button>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="OpenGitHubButton" Width="145" Height="36" Click="OpenGitHubButton_Click">Abrir GitHub</Button>
        </StackPanel>
    </Grid>
</UserControl>
