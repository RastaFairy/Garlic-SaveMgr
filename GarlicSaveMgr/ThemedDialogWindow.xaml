<Window x:Class="GarlicSaveMgr.ThemedDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Garlic SaveMgr"
        Width="{DynamicResource Theme.DialogWidth}"
        SizeToContent="Height"
        MinWidth="{DynamicResource Theme.DialogMinWidth}"
        MaxWidth="{DynamicResource Theme.DialogMaxWidth}"
        MinHeight="190"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource AppBackground}"
        Foreground="{DynamicResource TextPrimary}"
        FontFamily="Segoe UI"
        FontSize="13">
    <Grid Margin="{DynamicResource Theme.DialogPadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border x:Name="SeverityBar" Grid.RowSpan="3" Grid.Column="0" Background="{DynamicResource Accent}" CornerRadius="4" Margin="0,2,14,2" />
        <Border x:Name="IconCircle" Grid.Row="0" Grid.Column="1" Width="38" Height="38" CornerRadius="19"
                Background="{DynamicResource AccentSoft}" VerticalAlignment="Top" Margin="0,0,12,0">
            <TextBlock x:Name="IconText" Text="i" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource Accent}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <TextBlock x:Name="TitleText" Grid.Row="0" Grid.Column="2" Style="{StaticResource DialogTitle}" />
        <ScrollViewer Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,12,0,0"
                      MaxHeight="360" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <TextBlock x:Name="MessageText" Foreground="{DynamicResource TextSecondary}"
                       TextWrapping="Wrap" MaxWidth="620" />
        </ScrollViewer>
        <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="CancelButton" Width="100" Visibility="Collapsed" Click="CancelButton_Click">Cancelar</Button>
            <Button x:Name="OkButton" Width="112" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="OkButton_Click">Aceptar</Button>
        </StackPanel>
    </Grid>
</Window>
