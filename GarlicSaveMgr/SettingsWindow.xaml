<Window x:Class="GarlicSaveMgr.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajustes - Garlic SaveMgr" Width="560" Height="360"
        MinWidth="520" MinHeight="330" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize" Background="#F5F7FB" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="TextPrimary" Color="#162033" />
        <SolidColorBrush x:Key="TextSecondary" Color="#64748B" />
        <SolidColorBrush x:Key="Surface" Color="#FFFFFF" />
        <SolidColorBrush x:Key="BorderSoft" Color="#D8DEE8" />
        <SolidColorBrush x:Key="Accent" Color="#3454D1" />
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Background" Value="{StaticResource Surface}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#F7F8FC" /></Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource Accent}" />
            <Setter Property="BorderBrush" Value="{StaticResource Accent}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
            <Setter Property="Padding" Value="10,8" />
        </Style>
        <Style TargetType="Label"><Setter Property="Foreground" Value="{StaticResource TextSecondary}" /><Setter Property="VerticalContentAlignment" Value="Center" /></Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/><RowDefinition Height="20"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" CornerRadius="14" Padding="16,12">
            <StackPanel>
                <TextBlock Text="Configuración de consola" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" />
                <TextBlock Text="Configura la consola Garlic y comprueba la conexión." Margin="0,3,0,0" Foreground="{StaticResource TextSecondary}" />
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" CornerRadius="14" Padding="16">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0">Nombre</Label><TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" />
                <Label Grid.Row="1" Grid.Column="0" Margin="0,8,0,0">IP</Label><TextBox Grid.Row="1" Grid.Column="1" x:Name="IpBox" Margin="0,8,0,0" />
                <Label Grid.Row="2" Grid.Column="0" Margin="0,8,0,0">Puerto</Label><TextBox Grid.Row="2" Grid.Column="1" x:Name="PortBox" Text="8082" Margin="0,8,0,0" />
                <Button Grid.Row="3" Grid.Column="1" Margin="0,12,0,0" Click="Ping_Click">Verificar conexión</Button>
            </Grid>
        </Border>
        <Border Grid.Row="4" Background="#EEF2F7" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" CornerRadius="10" Padding="12">
            <StackPanel>
                <TextBlock Text="Aplicación portable" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" />
                <TextBlock TextWrapping="Wrap" Margin="0,3,0,0" Foreground="{StaticResource TextSecondary}"
                           Text="Los datos, copias, carátulas, logs y caché del payload se guardan en la carpeta de la aplicación (junto al ejecutable). La carpeta debe ser escribible." />
                <TextBlock TextWrapping="Wrap" Margin="0,6,0,0" Foreground="{StaticResource TextSecondary}"
                           Text="Para aplicar los cambios pulsa Aceptar." />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Width="95" Click="Cancel_Click">Cancelar</Button><Button Width="95" Style="{StaticResource PrimaryButton}" Click="Ok_Click">Aceptar</Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>